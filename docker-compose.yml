services:
  backend:
    build: ./backend
    ports:
      - "8080:8000"
    env_file: .env
    volumes:
      - ./backend:/app
      - ./data:/app/data
      - ./scripts:/app/scripts
      - ./utils:/app/utils
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
